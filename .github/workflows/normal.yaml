name: print hello world

on: workflow_dispatch

jobs:
  say helllo:
    runs-on: ubuntu-latest
    steps:
      -name: wellcome grettings
      run: echo "hello,gtihub actions"

      -name: same like grettings
      run: echo "wellcome"
      

